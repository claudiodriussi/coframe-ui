node_modules

# Output
.output
.vercel
.netlify
.wrangler
.svelte-kit
# The compiled site of each client. Narrow on purpose: a bare `build` also
# matched packages/coframe-ui/build/, which is not output at all — it holds the
# vite/kit factories every client imports, so a fresh clone could not build.
apps/*/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

